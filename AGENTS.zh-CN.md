# 贡献者与 Agent 工作约定

[English](AGENTS.md) · [简体中文](AGENTS.zh-CN.md)

本仓库只面向 Codex。任务领域可以变化，执行平台保持明确；不增加其他平台适配层或第三方 Skill/插件依赖。

文档权威顺序：用户最新明确指令、`docs/13-decisions.md` 中的已接受决策、正式定义与专题设计、模板和示例、临时讨论约定。核心概念、协议或生命周期变化时，同步更新决策与对应中英文说明。

主任务负责整体计划、依赖、分配、集成与最终结论。默认自己处理任务；必要独立审查或用户明确要求时再使用子代理。并行工作需要清楚边界、文件归属、输入输出和集成点，每个可变文件同时只有一个明确负责人。跨模块或高风险修改优先使用独立 worktree。

执行任务获得明确目标、范围、工作区、文件责任、相关输入、产物、验收与上报条件。共享事实保存在版本化定义、计划、状态与产物中；消息用于交流、通知与引用。验收核对实际版本、环境和证据，复用有效检查。

保留用户与其他任务的未提交成果，禁止破坏性恢复命令或重写共享历史，除非用户明确授权。交接保存源版本、工作区状态、改动、验证、剩余问题与继续位置。worktree 隔离目录，语义集成仍需要检查。

技能遵循渐进式披露：清楚的元数据、精炼入口、按需参考与脚本。每个技能负责明确能力，共享协议集中维护。保留触发条件、失败处理、可复现实例与验证。

完成需要实际产物、相关验证、可定位证据和同步文档。区分已确认事实、观察、设计决策与待验证假设；报告实际结果。运行程序使用原生 Codex，产品能力由真实链路验证。
