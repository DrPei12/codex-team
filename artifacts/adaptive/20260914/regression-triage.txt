2026-09-15

Direct pytest collection of all legacy tests was not a valid invocation for the existing plan projection fixture. Reproduced with:
python -B -m pytest -q --tb=short tests/test_team_plan.py::test_project_generates_briefs_from_one_canonical_manifest

Observed assertion:
ERROR: workspace_policy.artifact_root: artifact_root is outside experiment_root
1 failed in 2.84s

The test assigns artifact_root=tmp_path but its valid_manifest uses the fixed EXPERIMENT_ROOT. Its original main() creates TemporaryDirectory under EXPERIMENT_ROOT/runs. The canonical CI uses that main(), so the default pytest temp directory changes the test precondition. Product validation and test assertions were not weakened or changed.

The redundant full pytest invocation was stopped after this concrete reproduction. Validation continued with the nine existing standalone CI suites plus the new runtime pytest CI suite, with CI UTF-8 environment settings. See legacy-ci-20260915.txt and runtime-ci-20260915.txt for actual results.
