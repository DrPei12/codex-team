# AGENTS.md

本文件约束在本仓库内工作的所有 Codex 任务与 subagent。

## 最高优先级

1. 本项目只面向 Codex。不要加入平台无关抽象、adapter、Claude Code 支持或相应预留。
2. 先区分已确认事实、实验观察、设计决策、合理推测和待验证假设。不要把推测写成产品保证。
3. 项目已经立项，不再回到市场验证；工作重点是架构、实现、评测和验收。
4. 当前文档是项目基线。更改核心概念、默认范式、协议语义或生命周期政策时，同步更新决策日志和状态页。
5. 不因“可以并行”就默认并行。只有当任务边界、文件所有权、输入输出和集成点足够清楚时才并行。

## 文档权威顺序

发生冲突时依次采用：

1. 用户在当前主任务中的最新明确指令；
2. `docs/13-decisions.md` 中已接受且未被替代的决策；
3. 专题设计文档；
4. 模板和示例；
5. 对话中的临时约定。

`docs/15-conversation-record.md` 是决策级讨论记录，不是逐字 transcript。

## 多任务工作规则

- 主编排者拥有项目级计划、依赖图、任务分配、集成顺序和最终结论。
- 每个执行任务必须获得明确的目标、范围、禁止事项、workspace、文件所有权、输入、输出、验收条件和上报条件。
- 同一时刻一个可变文件只能有一个明确 owner；共享契约先定版或版本化。
- 高风险或跨模块变更优先使用独立 worktree。共享目录只用于只读调查或文件边界完全隔离的短任务。
- 不让多个任务凭自由文本维护彼此不一致的“项目真相”。项目真相写入版本库中的 spec、schema、计划、artifact 和证据文件。
- 消息用于通知、提问、阻塞和轻量状态；大段输出、补丁、日志和测试证据写入 artifact，再发送引用。
- worker 声称完成不等于主编排者验收完成。验收应验证身份、revision、环境、证据完整性和风险，但不得无理由重复昂贵全量测试。
- 临时任务完成并被接收后归档；长期模块 owner 可以保留；上下文老化时生成交接包并 handoff 给后继任务。

## Git 与修改安全

- 不覆盖用户或其他任务的未提交修改。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性恢复命令，除非用户明确要求。
- 并行实现必须使用文件所有权、独立分支/worktree 或串行集成来隔离。
- 不宣称 worktree 自动消除了语义冲突；它只隔离工作目录，合并仍需验证。
- 每次 handoff 必须写明源 revision、工作区状态、变更范围、测试证据、未决风险和下一步。

## 实现 skills 时

- 遵循 Codex skill 的渐进式披露：简短元数据 → 精炼 `SKILL.md` → 按需加载的 references/scripts/assets。
- 单个 skill 只负责一个清楚能力；公共协议、schema 和工具放在共享资源中，不复制多份。
- 不在单个 skill 目录堆放 README、过程笔记或重复文档。
- 新 skill 必须有触发条件、非触发条件、失败模式、可复现实例和验证。
- 在实测前，不把模型档位、上下文阈值、并发上限或节省比例写成固定常数。

## 完成定义

“完成”至少包含：目标产物存在、相关验证通过、证据可定位、残余风险已披露、状态和决策文档已同步。对外报告先给结论，再给证据和限制。
